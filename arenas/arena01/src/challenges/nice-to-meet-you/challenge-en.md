Write a file called NiceToMeetYou that displays Nice to meet you when using the cat command on it. It should not contain a \n character, such as:

```shell
$>cat -e NiceToMeetYou
Nice to meet you$>
```
