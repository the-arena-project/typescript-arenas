Write a function called reverseNumbers that takes an array of numbers and returns an array of numbers in the strictly reverse order without modifying the original array.

The function will be prototyped in the following way:

```
type ReverseNumbersFn = (numbers: number[]) => number[]
```
